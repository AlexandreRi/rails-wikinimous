<div class="content">
  <ul>
    <% @articles.each do |article| %>
      <li><%= link_to article.title, article_path(article), class: "btn-articles" %></li>
      <li><p><%= article.content %></p></li>
    <% end %>
  </ul>
</div>
